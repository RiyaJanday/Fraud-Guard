[pytest]
# Ensures `pytest` run from the project root (or anywhere else) always
# resolves `app.*` imports the same way `uvicorn app.main:app` does —
# without this, test discovery can succeed while `import app.models` fails
# depending on the current working directory.
pythonpath = .
testpaths = tests
