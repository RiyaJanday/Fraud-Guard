# ============================================================================
# FraudGuard Backend — Python dependencies
# Python 3.11+
# ============================================================================

# --- Web framework -----------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Settings / validation ----------------------------------------------
pydantic==2.10.5
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database -------------------------------------------------------------
sqlalchemy==2.0.37
psycopg2-binary==2.9.10
alembic==1.14.1

# --- Authentication / security --------------------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pinned below 4.1: passlib 1.7.4 (last release, unmaintained) reads a
# bcrypt.__about__.__version__ attribute that bcrypt>=4.1 removed. Using
# 4.0.1 avoids a noisy (harmless, but log-spamming) warning on every hash.
bcrypt==4.0.1

# --- Caching / rate limiting / real-time -----------------------------------
redis==5.2.1
slowapi==0.1.9
websockets==14.1

# --- Data science / ML ------------------------------------------------
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.6.1
# Pinned to 2.1.4, not 2.1.3: scikit-learn 1.6 introduced a new
# __sklearn_tags__ protocol that XGBClassifier only implements from 2.1.4
# onward. On 2.1.3, any sklearn meta-estimator wrapping XGBClassifier
# (RandomizedSearchCV, GridSearchCV, cross_val_score, etc.) crashes with
# AttributeError: 'super' object has no attribute '__sklearn_tags__'.
xgboost==2.1.4
imbalanced-learn==0.13.0
shap==0.46.0
joblib==1.4.2

# --- Reporting --------------------------------------------------------
reportlab==4.2.5

# --- Logging ------------------------------------------------------------
loguru==0.7.3

# --- Environment ----------------------------------------------------------
python-dotenv==1.0.1

# --- Testing --------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
